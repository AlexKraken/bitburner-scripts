/** 
 * Prints the player's current Karma to the terminal
 * The minimum requirement to join a gang is -54000
 * 
 * @param {NS} ns 
 */
export async function main(ns) {
    ns.tprint(ns.heart.break())
}